
# 🚀 نظام أتمتة بلوجر المتقدم بالذكاء الاصطناعي (AI Blogger Automation)

نظام برمجي متكامل للنشر التلقائي عالي الجودة على منصة **Blogger**. يقوم هذا النظام بمحاكاة دور فريق تحرير كامل (باحث، كاتب، مدقق، خبير SEO، ومصمم) لإنتاج محتوى احترافي دون أي تدخل بشري.

يعتمد النظام على **Google Gemini 2.5 Flash** مع بنية تحتية قوية لضمان الاستمرارية والجودة.

---

## ✨ المميزات الرئيسية

### 1. 🔄 نظام النشر المزدوج (Dual Mode)
لكل فئة (Category) تحددها، يقوم النظام بنشر نوعين من المقالات يومياً:
*   **مقال تريند (Trending):** يبحث في الإنترنت عن أحدث الأخبار والابتكارات (آخر 60 يوماً) ويكتب عنها تقريراً صحفياً.
*   **مقال دائم (Evergreen):** يكتب دليلاً تعليمياً شاملاً يشرح المفاهيم الأساسية (محتوى مرجعي طويل الأمد).

### 2. 🧠 سلسلة التفكير الخماسية (5-Step Chain)
لا يتم كتابة المقال بطلب واحد، بل يمر بـ 5 مراحل لضمان الجودة:
1.  **البحث (Research):** جمع المصادر والتحقق من الحقائق.
2.  **المسودة (Drafting):** الكتابة بأسلوب صحفي بشري (Human-like).
3.  **الاستراتيجية (SEO & Strategy):** تحسين الكلمات المفتاحية وبناء الروابط.
4.  **التدقيق (Audit):** مراجعة الأرقام، المصادر، وإزالة عبارات الذكاء الاصطناعي المكررة.
5.  **النشر (Publishing):** التنسيق النهائي وتجهيز HTML.

### 3. 🎨 صور ذكية وحصرية
*   يولد النظام صوراً تعبر عن عنوان المقال بدقة.
*   يكتب **نصاً توضيحياً (Overlay Text)** على الصورة لتبدو احترافية.
*   يستخدم خدمة **Pollinations** للتوليد و **ImgBB** للاستضافة الدائمة.

### 4. 🔗 روابط داخلية ذكية (Smart Internal Linking)
*   النظام يحتفظ بقاعدة بيانات (`knowledge_graph.json`) لكل ما نشره سابقاً.
*   أثناء الكتابة، يبحث عن مفاهيم لها مقالات سابقة ويقوم بربطها بـ **الرابط الحقيقي** للمقال (يمنع أخطاء 404).

### 5. 🛡️ استقرار تقني عالي (Enterprise Grade)
*   **6 مفاتيح API:** نظام تدوير تلقائي للمفاتيح لتجاوز حدود الاستخدام اليومي (Rate Limits).
*   **أرشفة تلقائية:** ينقل المقالات القديمة للأرشيف سنوياً للحفاظ على سرعة النظام.
*   **حفظ آمن (Safety Save):** يضمن حفظ البيانات في GitHub حتى لو انقطع الاتصال أثناء العمل.
*   **تصميم CSS:** يحقن كود تصميم يجعل المقالات تبدو عصرية ومريحة للقراءة.

---

## 🛠️ المتطلبات المسبقة

1.  حساب **GitHub**.
2.  حساب **Google** (لخدمات Blogger و Gemini).
3.  حساب **ImgBB** (لاستضافة الصور).

---

## ⚙️ دليل الإعداد الشامل (خطوة بخطوة)

### الخطوة 1: الحصول على مفاتيح Gemini API
1.  اذهب إلى [Google AI Studio](https://aistudio.google.com/).
2.  أنشئ **6 مفاتيح API** مختلفة (لضمان عمل النظام 24/7 دون توقف).
3.  احتفظ بالمفاتيح في ملف نصي مؤقتاً.

### الخطوة 2: الحصول على مفتاح ImgBB
1.  اذهب إلى [ImgBB API](https://api.imgbb.com/).
2.  اضغط على "Get API Key".
3.  انسخ المفتاح.

### الخطوة 3: إعداد Blogger API (الجزء الأهم)
تحتاج إلى استخراج 4 قيم: `Client ID`, `Client Secret`, `Blog ID`, و `Refresh Token`.

**أ. الحصول على Blog ID:**
*   افتح لوحة تحكم مدونتك في Blogger.
*   انظر إلى رابط الصفحة في المتصفح: `blogger.com/blog/post/edit/123456789...`
*   الرقم الطويل `123456789...` هو **BLOGGER_BLOG_ID**.

**ب. الحصول على Client ID & Secret:**
1.  اذهب إلى [Google Cloud Console](https://console.cloud.google.com/).
2.  أنشئ مشروعاً جديداً (New Project).
3.  من القائمة الجانبية، اختر **APIs & Services > Library**.
4.  ابحث عن "Blogger API v3" واضغط **Enable**.
5.  اذهب إلى **OAuth consent screen**:
    *   اختر **External**.
    *   املأ البيانات المطلوبة (اسم التطبيق، الإيميل).
    *   في قسم **Test Users**، أضف بريدك الإلكتروني (خطوة ضرورية).
6.  اذهب إلى **Credentials** -> **Create Credentials** -> **OAuth Client ID**.
    *   نوع التطبيق: **Desktop App**.
    *   انسخ **Client ID** و **Client Secret**.

**ج. الحصول على Refresh Token:**
1.  اذهب إلى [Google OAuth 2.0 Playground](https://developers.google.com/oauthplayground/).
2.  اضغط على أيقونة الترس ⚙️ (أعلى اليمين).
3.  فعل خيار "Use your own OAuth credentials".
4.  الصق الـ **Client ID** و **Client Secret** الخاصين بك.
5.  في القائمة اليسرى، ابحث عن "Blogger API v3" واختر `https://www.googleapis.com/auth/blogger`.
6.  اضغط **Authorize APIs**.
7.  سجل الدخول بحساب جوجل الخاص بالمدونة ووافق على الصلاحيات (إذا ظهر تحذير "Unverified app"، اضغط Advanced ثم Go to... unsafe).
8.  اضغط زر **Exchange authorization code for tokens**.
9.  انسخ كود **Refresh Token** الطويل.

---

### الخطوة 4: إعداد المستودع في GitHub

1.  اذهب إلى إعدادات المستودع (Settings).
2.  من القائمة الجانبية: **Secrets and variables > Actions**.
3.  اضغط **New repository secret** وأضف المفاتيح التالية بدقة:

| اسم السر (Secret Name) | القيمة (Value) |
| :--- | :--- |
| `GEMINI_API_KEY_1` | مفتاح Gemini الأول |
| `GEMINI_API_KEY_2` | مفتاح Gemini الثاني |
| ... | ... (أكمل حتى المفتاح السادس) |
| `GEMINI_API_KEY_6` | مفتاح Gemini السادس |
| `IMGBB_API_KEY` | مفتاح ImgBB |
| `BLOGGER_BLOG_ID` | معرف المدونة |
| `BLOGGER_CLIENT_ID` | Client ID من جوجل كلاود |
| `BLOGGER_CLIENT_SECRET` | Client Secret من جوجل كلاود |
| `BLOGGER_REFRESH_TOKEN` | Refresh Token الذي استخرجته |

---

### الخطوة 5: ضبط المحتوى (`config_advanced.json`)

افتح ملف `config_advanced.json` وعدله ليناسب تخصص مدونتك.

```json
{
  "categories": {
    "Technology": {
      "trending_focus": "Latest gadgets, smartphones, AI news...",
      "evergreen_prompt": "Write a guide about how computers work..."
    },
    "Health": {
      "trending_focus": "Medical breakthroughs, WHO updates...",
      "evergreen_prompt": "Write a guide about healthy nutrition..."
    }
  }
}

ملاحظة: أسماء الفئات (Keys) هي التي ستظهر كتصنيفات (Labels) في بلوجر.

🚀 كيفية التشغيل
التشغيل التلقائي (Automatic)

النظام مضبوط ليعمل تلقائياً كل يوم الساعة 09:00 UTC عبر GitHub Actions.

التشغيل اليدوي (Manual)

اذهب إلى تبويب Actions في GitHub.

اختر Daily Article Publishing من القائمة اليسرى.

اضغط على زر Run workflow (الزر الرمادي على اليمين).

📂 شرح الملفات

article_generator_advanced.py: المحرك الرئيسي. يحتوي على كود البايثون، إدارة المفاتيح، توليد الصور، ومنطق النشر.

config_advanced.json: ملف التحكم في الأقسام والمواضيع.

knowledge_graph.json: قاعدة بيانات المقالات المنشورة (يتم تحديثها تلقائياً لمنع التكرار ولعمل الروابط الداخلية).

archive/: مجلد يتم إنشاؤه تلقائياً لحفظ أرشيف السنوات السابقة (للحفاظ على خفة النظام).

.github/workflows/daily-publish.yml: ملف إعدادات الأتمتة (Cron Job).

❓ حل المشاكل الشائعة (Troubleshooting)

1. المقالات نُشرت لكن لا توجد روابط داخلية؟

الروابط الداخلية تتطلب وجود "تاريخ" للمدونة. في اليوم الأول، الملف knowledge_graph.json يكون فارغاً. بدءاً من اليوم الثاني، سيبدأ النظام بربط المقالات الجديدة بالمقالات القديمة.

2. الصور لا تظهر في المقال؟

تأكد من صحة IMGBB_API_KEY. النظام يحاول 3 مرات، إذا فشل الاتصال أو كان المفتاح خطأ، سينشر المقال بدون صورة لتجنب توقف العمل.

3. خطأ في GitHub Actions (Permission denied)؟

تأكد من أن ملف الـ Workflow يحتوي على:

code
Yaml
download
content_copy
expand_less
permissions:
  contents: write

وهذا موجود بالفعل في الكود المرفق.

تم التطوير ليعمل بكفاءة قصوى واستقرار تام. استمتع بالتدوين الآلي! 🤖✍️

code
Code
download
content_copy
expand_less
